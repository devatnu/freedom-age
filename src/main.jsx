import React from 'react';
import ReactDOM from 'react-dom/client';
import FreedomAge from './FreedomAge';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode><FreedomAge /></React.StrictMode>
);
